# GitHub Preferences

- Prefer `gh` CLI tool over GitHub API for creating PRs and other GitHub
  operations
- Some organizations have OAuth App access restrictions that block the
  GitHub API tool; see `local.md`

